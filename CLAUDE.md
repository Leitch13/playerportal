# Player Portal

Before changing anything under `src/app/api/stripe`, `src/app/api/migration`, `src/lib/billing`, or any signup/booking page: read `BILLING_RULES.md` and follow it. Billing is one rule and one code path for every academy, and it changes only on John's explicit written decision.
